Bootstrap: docker
From: astral/uv:python3.12-bookworm-slim

%files
    example.py /app/example.py
    iris.data /app/iris.data
    requirements.txt /app/requirements.txt

%post
    mkdir /output
    cd /app
    uv venv
    uv pip install --requirements requirements.txt

%runscript
    cd /app
    exec uv run -p .venv example.py
